[project]
name = "info-segugio"
version = "0.1.0"
description = ""
authors = [
    {name = "“Ivan",email = "“ivanschena@gmail.com”"}
]
readme = "README.md"
requires-python = ">=3.10,<3.14.0"
dependencies = [
    "yarl (>=1.9,<1.20)",
    "chainlit (>=2.11.1,<3.0.0)",
    "openai (>=2.38.0,<3.0.0)"
]

[tool.poetry]
packages = [{include = "info_segugio", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
